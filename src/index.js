import React from "react";
import ReactDOM from "react-dom";
import "./index.css";

const App = () => (
  <div className="app">
    <h2>Welcome to create react parcel app!</h2>
  </div>
);

ReactDOM.render(<App />, document.getElementById("root"));
