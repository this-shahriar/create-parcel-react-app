{
  "name": "create-parcel-react-app",
  "version": "1.0.0",
  "description": "Alternative for Create-React-App with zero configuration Parcel-Bundler",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shahriar Hasan Parash",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
